<odoo>
    <data>
        <record model="ir.actions.act_window" id="size_action_window">
            <field name="name">kki_lift_size window</field>
            <field name="res_model">kki_lift.size</field>
            <field name="view_mode">tree,form</field>
        </record>

        <menuitem name="size" id="kki_lift.size_list" parent="kki_lift.lift"
                  action="size_action_window"/>
    </data>
</odoo>